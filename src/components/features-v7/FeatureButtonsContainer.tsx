"use client";

/**
 * FeatureButtonsContainer - Previously rendered floating circular buttons on the screen overlay.
 * Removed to keep the player and lyrics view completely clean, transparent, and distraction-free.
 * All professional tools, audio effects, A-B loop, visualization, and desktop lyrics are now accessible via Settings and the Header Toolbar.
 */
export function FeatureButtonsContainer() {
  return null;
}
